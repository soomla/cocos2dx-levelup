paths=( "submodules/ios-levelup" "soomla-native/projects/cocos2dx-ios-levelup" )
build=../build/ios

rm -rf $build
mkdir -p $build

for path in "${paths[@]}"
do
  cd $path
  ./build_all
  cd -
  cp -R $path/build/* $build/
done

